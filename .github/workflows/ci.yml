name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run conformance suite (interp + vm)
        run: |
          pip install pytest
          python -m pytest tests -v
      - name: Smoke demo
        run: python cli.py examples/demo.fl 2>/dev/null || python _smoke.py
